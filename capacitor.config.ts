import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.drsaeedmoghadam.app",
  appName: "دکتر سعید مقدم",
  webDir: "dist/demo/browser",
  server: {
    androidScheme: "https",
  },
};

export default config;
