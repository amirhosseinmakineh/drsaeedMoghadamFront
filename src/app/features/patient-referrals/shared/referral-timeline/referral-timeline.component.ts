import { CommonModule } from "@angular/common";
import { Component, Input } from "@angular/core";
import { PatientReferral, PatientReferralStatus } from "../../models/patient-referral.models";
@Component({ selector:"app-referral-timeline", standalone:true, imports:[CommonModule], template:`
<ol class="timeline" aria-label="روند معرفی"><li *ngFor="let step of steps; let i=index" [class.done]="isDone(i)" [class.rejected]="referral.status===statuses.Rejected && i===reviewIndex"><i></i><span>{{step}}</span></li></ol>
<p class="reject" *ngIf="referral.status===statuses.Rejected">دلیل رد: {{referral.rejectionReason || 'ثبت نشده'}}</p>`, styles:[`
.timeline{list-style:none;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));padding:0;margin:1rem 0;gap:.25rem}.timeline li{min-width:0;text-align:center;color:#94a3b8;font-size:.72rem;position:relative}.timeline i{display:block;width:.75rem;height:.75rem;margin:0 auto .4rem;border-radius:50%;background:#cbd5e1}.timeline li:not(:last-child):after{content:'';position:absolute;top:.35rem;right:55%;width:90%;height:2px;background:#cbd5e1}.timeline .done{color:#166534}.timeline .done i,.timeline .done:not(:last-child):after{background:#22c55e}.timeline .rejected{color:#991b1b}.timeline .rejected i{background:#ef4444}.reject{background:#fff1f2;color:#9f1239;padding:.65rem;border-radius:.6rem}@media(max-width:480px){.timeline{grid-template-columns:1fr}.timeline li{display:flex;gap:.6rem;text-align:right;align-items:center}.timeline i{margin:.3rem}.timeline li:after{display:none}}` ]})
export class ReferralTimelineComponent { @Input({required:true}) referral!:PatientReferral; readonly statuses=PatientReferralStatus; readonly steps=["ثبت معرفی","تماس منشی","ثبت رزرو","بررسی ادمین","پرداخت پاداش"]; readonly reviewIndex=3; isDone(i:number){ const s=this.referral.status; if(s===PatientReferralStatus.Rejected) return i===0 || !![this.referral.contactedAt,this.referral.reservationAt][i-1]; return i <= ({1:0,2:1,3:2,4:4}[s] ?? 0); } }
