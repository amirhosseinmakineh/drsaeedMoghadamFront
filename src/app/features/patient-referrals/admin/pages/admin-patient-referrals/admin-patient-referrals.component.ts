import { CommonModule } from "@angular/common";import { ChangeDetectionStrategy,ChangeDetectorRef,Component,OnInit } from "@angular/core";import { FormsModule } from "@angular/forms";import { RouterLink } from "@angular/router";import { finalize } from "rxjs";import { ToastService } from "../../../../../core/toast/toast.service";import { BaseDialogComponent,EmptyStateComponent,LoadingComponent } from "../../../../../shared/base";import { PagedReferrals,PatientReferral,PatientReferralStatus,ReferralQuery,REFERRAL_STATUS_LABELS } from "../../../models/patient-referral.models";import { PatientReferralApiService } from "../../../services/patient-referral-api.service";import { ReferralCardComponent } from "../../../shared/referral-card/referral-card.component";
@Component({selector:"app-admin-patient-referrals",standalone:true,imports:[CommonModule,FormsModule,RouterLink,BaseDialogComponent,EmptyStateComponent,LoadingComponent,ReferralCardComponent],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:"./admin-patient-referrals.component.html",styleUrl:"../../../patient-referrals.scss"})
export class AdminPatientReferralsComponent implements OnInit{list:PagedReferrals|null=null;loading=false;busyId:number|null=null;dialog:"approve"|"reject"|null=null;selected:PatientReferral|null=null;reason="";query:ReferralQuery={page:1,pageSize:10,status:PatientReferralStatus.ReservedPendingAdminApproval,search:""};readonly S=PatientReferralStatus;readonly statuses=Object.entries(REFERRAL_STATUS_LABELS).map(([value,label])=>({value:+value,label}));constructor(private api:PatientReferralApiService,private toast:ToastService,private cdr:ChangeDetectorRef){}ngOnInit(){this.load()}load(){this.loading=true;this.api.adminReferrals(this.query).pipe(finalize(()=>{this.loading=false;this.cdr.markForCheck()})).subscribe({next:r=>this.list=r,error:()=>this.toast.error("دریافت معرفی‌ها انجام نشد.")})}filter(){this.query.page=1;this.load()}open(item:PatientReferral,kind:"approve"|"reject"){if(item.status!==this.S.ReservedPendingAdminApproval)return;this.selected=item;this.reason="";this.dialog=kind}review(){if(!this.selected||this.busyId)return;if(this.dialog==="reject"&&!this.reason.trim()){this.toast.error("دلیل رد را وارد کنید.");return}this.busyId=this.selected.id;const approve=this.dialog==="approve";this.api.review(this.selected.id,approve,approve?undefined:this.reason.trim()).pipe(finalize(()=>{this.busyId=null;this.cdr.markForCheck()})).subscribe({next:updated=>{Object.assign(this.selected!,updated,{status:approve?this.S.ApprovedRewarded:this.S.Rejected,rejectionReason:approve?null:this.reason.trim()});this.dialog=null;this.toast.success(approve?"معرفی تأیید و پاداش پرداخت شد.":"معرفی رد شد.")},error:()=>this.toast.error("ثبت بررسی انجام نشد.")})}name(item:PatientReferral){return `${item.firstName} ${item.lastName}`}referrer(item:PatientReferral){return item.referrerFullName||`${item.referrerFirstName||""} ${item.referrerLastName||""}`.trim()||"بیمار معرفی‌کننده"}}
