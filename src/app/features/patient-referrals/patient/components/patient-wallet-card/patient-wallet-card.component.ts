import { DecimalPipe } from "@angular/common";import { Component,Input } from "@angular/core";
@Component({selector:"app-patient-wallet-card",standalone:true,imports:[DecimalPipe],template:`<section><span>کیف پول شما</span><strong>{{balance|number:'1.0-0'}} تومان</strong><div><p>معرفی تأییدشده: {{approved}}</p><p>پاداش هر معرفی: {{1000000|number:'1.0-0'}} تومان</p></div></section>`,styles:[`section{box-sizing:border-box;width:100%;min-width:0;background:linear-gradient(135deg,#14532d,#16a34a);color:#fff;border-radius:1rem;padding:1.25rem}strong{display:block;font-size:clamp(1.4rem,5vw,2rem);margin:.5rem 0;overflow-wrap:anywhere}div{display:flex;gap:1rem;flex-wrap:wrap}p{margin:.25rem 0}`]}) export class PatientWalletCardComponent{@Input() balance=0;@Input() approved=0}
